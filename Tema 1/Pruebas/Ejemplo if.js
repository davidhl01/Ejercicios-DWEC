var mivariable = " ";

if (mivariable) {
    // alert("jose");
    document.write(`Variable contains ${mivariable}`);
}

// === sirve para comprobar que son del mismo tipo y valen lo mismo.

ejercicio1();